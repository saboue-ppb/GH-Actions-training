name: GitHub Actions Demo
on:
  push:
    branches: 
      - main
  pull_request:
    branches: 
      - main
  schedule:
    - cron: '15 6 * * SUN'

jobs:
  build:
    runs-on: ubuntu-latest
    name: GitHub Actions Demo
    
